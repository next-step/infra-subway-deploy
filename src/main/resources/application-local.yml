spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/subway?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: masterpw
  jpa:
    hibernate:
      ddl-auto: validate

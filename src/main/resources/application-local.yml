spring:
  datasource:
    url: jdbc:mysql://localhost:3306/subway
    username: root
    password: masterpw
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      format_sql: true
      show_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
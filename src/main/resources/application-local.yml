spring:
  datasource:
    url: jdbc:mysql://localhost:3306/subway
    username: root
    password: masterpw
  jpa:
    hibernate:
      ddl-auto: validate
    generate-ddl: false
  flyway:
    enabled: true
    baseline-on-migrate: true

spring:
  profiles:
    active: env
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/local-db
    username: root
    password: 1234
  flyway:
    enabled: true
    baseline-on-migrate: true
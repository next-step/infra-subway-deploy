spring:
  profiles:
    active: prod
  jpa:
    properties:
      hibernate:
        show_sql: false
        format_sql: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://192.148.0.143:3306/subway?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: masterpw

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: error